# URL base de la API (Render). Incluye /api/v1 al final, sin barra final.
# En desarrollo NO setear esta variable — Vite usa el proxy local.
VITE_API_URL="https://taller-tohan-backend.onrender.com/api/v1"
